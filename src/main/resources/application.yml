spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: ALWAYS
  profiles:
    default: development
rsa:
  private-key: classpath:certs/private_key.pem
  public-key: classpath:certs/public_key.pem